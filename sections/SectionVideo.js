import { t } from '@lingui/macro';

import Container from '@/components/Container';

export default function SectionVideo({ id, bgcolor }) {
  return <Container title={t`lxdao-intro-video-title`}>sfasdf</Container>;
}
