import { t } from '@lingui/macro';

import Container from '@/components/Container';

export default function SectionMission({ id, bgcolor }) {
  return <Container title={t`lxdao-mission-title`}>sfasdf</Container>;
}
