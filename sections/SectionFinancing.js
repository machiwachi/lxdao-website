import { t } from '@lingui/macro';

import Container from '@/components/Container';

export default function SectionFinancing({ id, bgcolor }) {
  return <Container title={t`lxdao-financing-title`}>sfasdf</Container>;
}
